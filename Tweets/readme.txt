-- Readme --
To run the app, unzip the project first.

Install npm from https://www.npmjs.com/get-npm

Run the following command to install global packages:
npm install -g twitter-proxy
npm install -g typescript
npm install -g live-server

cd into tweets folder, and run the following command:
twitter-proxy twitter_proxy_config.json

Open tweets/index.html to view