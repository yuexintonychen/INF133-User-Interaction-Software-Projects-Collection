-- Readme --
To run the app, unzip the project first.

Install npm from https://www.npmjs.com/get-npm

Run the following command to install global packages:
npm install -g ionic
npm install -g cordova

cd into sleeptracker folder directory and run the following command:
npm install
ionic lab

Open localhost:8100 in the browser to view.